

export { Solver };

class Solver {
    name: string;
    node: number[]; // node on which the solver is working

    constructor(name:string) {
        this.name = name;
        this.node = [];

    }
};
