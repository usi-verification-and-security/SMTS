*This is a draft!*
If you have any questions please write to matteo.marescotti@usi.ch


SMTServer is an SMT solver built for distributed computing environment.
The tool is currently under development, with some restricted functionalities.
Available SMT solver engines:
 - OpenSMT2
 - Spacer (Z3 engine, without clause sharing)

To compile it:
$ mkdir build
$ cd build
$ cmake ..

# The output should warning if something has not been found, and guide the user on how to specify where to find them if needed.

$ makes